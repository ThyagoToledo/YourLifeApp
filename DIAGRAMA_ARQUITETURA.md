# 🎨 DIAGRAMA VISUAL DA ARQUITETURA - YourLife Android

**Data:** 04/11/2025

---

## 📊 ARQUITETURA COMPLETA (MVVM)

```
┌───────────────────────────────────────────────────────────────────────┐
│                         🖥️  PRESENTATION LAYER                         │
│                              (UI / View)                               │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐      │
│  │  MainActivity   │  │  FeedActivity   │  │ FeedFragment    │      │
│  │                 │  │                 │  │                 │      │
│  │  • Login UI     │  │  • Feed UI      │  │  • RecyclerView │      │
│  │  • Register UI  │  │  • Create Post  │  │  • Adapter      │      │
│  │  • Validation   │  │  • Pull Refresh │  │  • Item Layout  │      │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘      │
│           │                    │                     │                │
│           │  ViewBinding       │                     │                │
│           ▼                    ▼                     ▼                │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │             activity_main.xml   activity_feed.xml            │     │
│  │             item_post.xml   fragment_feed.xml                │     │
│  └──────────────────────────────────────────────────────────────┘     │
│                                                                        │
└───────────────────────────────┬────────────────────────────────────────┘
                                │
                                │ LiveData.observe()
                                ▼
┌───────────────────────────────────────────────────────────────────────┐
│                       🧠  VIEWMODEL LAYER                              │
│                        (Business Logic)                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐      │
│  │  AuthViewModel  │  │  FeedViewModel  │  │ ProfileViewModel│      │
│  │                 │  │                 │  │                 │      │
│  │  • login()      │  │  • loadFeed()   │  │  • getProfile() │      │
│  │  • register()   │  │  • createPost() │  │  • update()     │      │
│  │  • isAuth()     │  │  • likePost()   │  │  • uploadAvatar()│      │
│  │                 │  │  • comment()    │  │                 │      │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘      │
│           │                    │                     │                │
│           │                    │                     │                │
│           │   MutableLiveData  │                     │                │
│  ┌────────┴────────────────────┴─────────────────────┴────────┐      │
│  │                                                              │      │
│  │   _loginState    _feedState    _createPostState             │      │
│  │   LiveData<Resource<AuthResponse>>                          │      │
│  │   LiveData<Resource<List<Post>>>                            │      │
│  │                                                              │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                        │
└───────────────────────────────┬────────────────────────────────────────┘
                                │
                                │ repository.method()
                                │ viewModelScope.launch { }
                                ▼
┌───────────────────────────────────────────────────────────────────────┐
│                      💾  DATA LAYER                                    │
│                    (Repository Pattern)                                │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │              YourLifeRepository                               │    │
│  │                                                               │    │
│  │  • login(email, password)                                     │    │
│  │  • register(name, email, password)                            │    │
│  │  • getFeed(token)                                             │    │
│  │  • createPost(token, content)                                 │    │
│  │  • likePost(token, postId)                                    │    │
│  │  • getFriends(token)                                          │    │
│  │  • sendFriendRequest(token, userId)                           │    │
│  │  • getMessages(token, userId)                                 │    │
│  │                                                               │    │
│  │  Private:                                                     │    │
│  │  • handleResponse<T>(response): Resource<T>                   │    │
│  │  • withContext(Dispatchers.IO) { }                            │    │
│  │                                                               │    │
│  └──────────────────────────┬────────────────────────────────────┘    │
│                             │                                          │
│                             │ api.method()                             │
│                             ▼                                          │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │                  RetrofitInstance                             │    │
│  │                                                               │    │
│  │  • BASE_URL: https://your-life-gamma.vercel.app/api/         │    │
│  │  • OkHttpClient (Logging, Timeout)                           │    │
│  │  • Gson Converter                                             │    │
│  │  • val api: ApiService                                        │    │
│  │                                                               │    │
│  └──────────────────────────┬────────────────────────────────────┘    │
│                             │                                          │
└─────────────────────────────┼──────────────────────────────────────────┘
                              │
                              │ HTTP Request
                              ▼
┌───────────────────────────────────────────────────────────────────────┐
│                      🌐  NETWORK LAYER                                 │
│                      (API Interface)                                   │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │                    ApiService (Interface)                     │    │
│  │                                                               │    │
│  │  @POST("auth/login")                                          │    │
│  │  suspend fun login(@Body request): Response<AuthResponse>     │    │
│  │                                                               │    │
│  │  @GET("feed")                                                 │    │
│  │  suspend fun getFeed(@Header token): Response<List<Post>>     │    │
│  │                                                               │    │
│  │  @POST("posts")                                               │    │
│  │  suspend fun createPost(@Header, @Body): Response<...>        │    │
│  │                                                               │    │
│  │  @POST("posts/{id}/like")                                     │    │
│  │  suspend fun likePost(@Header, @Path id): Response<...>       │    │
│  │                                                               │    │
│  │  @GET("friends")                                              │    │
│  │  @POST("friends/request")                                     │    │
│  │  @GET("messages/conversations")                               │    │
│  │  @POST("messages")                                            │    │
│  │  ... (total: ~20 endpoints)                                   │    │
│  │                                                               │    │
│  └──────────────────────────┬────────────────────────────────────┘    │
│                             │                                          │
└─────────────────────────────┼──────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌───────────────────────────────────────────────────────────────────────┐
│                      ☁️  BACKEND API                                   │
│             https://your-life-gamma.vercel.app/api/                   │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  • Node.js + Express                                                  │
│  • PostgreSQL Database                                                │
│  • JWT Authentication                                                 │
│  • RESTful API                                                        │
│                                                                        │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 FLUXO DE DADOS (Data Flow)

### **1. Login Flow (Exemplo Completo)**

```
┌────────────┐  1. Usuário digita email/senha
│            │     e clica em "Login"
│ MainActivity│◄────────────────────────────────────┐
│            │                                      │
└──────┬─────┘                                      │
       │                                            │
       │ 2. binding.btnLogin.setOnClickListener    │
       │                                            │
       ▼                                            │
┌────────────┐  3. authViewModel.login(email, pass)│
│ AuthView   │                                      │
│  Model     │                                      │
└──────┬─────┘                                      │
       │                                            │
       │ 4. viewModelScope.launch {                │
       │      _loginState.value = Resource.Loading()
       │                                            │
       ▼                                            │
┌────────────┐  5. repository.login(email, pass)   │
│ YourLife   │                                      │
│ Repository │                                      │
└──────┬─────┘                                      │
       │                                            │
       │ 6. withContext(Dispatchers.IO) {          │
       │      val response = api.login(...)        │
       │                                            │
       ▼                                            │
┌────────────┐  7. HTTP POST /auth/login           │
│ ApiService │     Body: { email, password }       │
│  (Retrofit)│                                      │
└──────┬─────┘                                      │
       │                                            │
       │ 8. HTTPS Request                          │
       │                                            │
       ▼                                            │
┌────────────┐  9. Backend processa login          │
│  Backend   │     Verifica credenciais            │
│    API     │     Gera JWT Token                  │
└──────┬─────┘                                      │
       │                                            │
       │ 10. Response JSON:                        │
       │     { success: true, token: "...",        │
       │       user: { id, name, email } }         │
       │                                            │
       ▼                                            │
┌────────────┐  11. Gson converte JSON → Kotlin    │
│  Gson      │      AuthResponse(success, token)   │
│ Converter  │                                      │
└──────┬─────┘                                      │
       │                                            │
       │ 12. Repository encapsula:                 │
       │     Resource.Success(authResponse)        │
       │                                            │
       ▼                                            │
┌────────────┐  13. ViewModel salva token:         │
│ AuthView   │      TokenManager.saveToken(...)    │
│  Model     │      _loginState.value = Success    │
└──────┬─────┘                                      │
       │                                            │
       │ 14. LiveData notifica Observer            │
       │                                            │
       ▼                                            │
┌────────────┐  15. when (loginState) {            │
│ MainActivity│       is Success → navegar          │
│  .observe  │       is Error → mostrar erro       │
└────────────┘       is Loading → mostrar spinner  │
       │                                            │
       │ 16. startActivity(FeedActivity)           │
       └────────────────────────────────────────────┘
```

---

## 🗂️ ESTRUTURA DE ARQUIVOS

```
📁 app/src/main/java/com/example/yourlife/
│
├── 📁 data/
│   ├── 📁 model/                    ← Data Classes
│   │   ├── User.kt                  (User, LoginRequest, RegisterRequest, AuthResponse)
│   │   ├── Post.kt                  (Post, CreatePostRequest, Comment)
│   │   ├── Message.kt               (Message, Conversation, Notification)
│   │   └── Friend.kt                (FriendRequest, FriendshipStatus)
│   │
│   ├── 📁 remote/                   ← API Configuration
│   │   ├── ApiService.kt            (Interface com todos os endpoints)
│   │   └── RetrofitInstance.kt      (Singleton do Retrofit)
│   │
│   └── 📁 repository/               ← Repository Pattern
│       └── YourLifeRepository.kt    (Centraliza acesso a dados)
│
├── 📁 ui/                           ← Presentation Layer
│   ├── 📁 auth/
│   │   └── AuthViewModel.kt         (Login/Registro)
│   │
│   ├── 📁 feed/
│   │   ├── FeedActivity.kt          (Tela principal do feed)
│   │   ├── FeedFragment.kt          (Fragment do feed)
│   │   ├── FeedViewModel.kt         (Lógica do feed)
│   │   └── FeedAdapter.kt           (RecyclerView adapter)
│   │
│   ├── 📁 friends/
│   │   ├── FriendsFragment.kt       (Lista de amigos)
│   │   ├── FriendAdapter.kt         (Adapter de amigos)
│   │   └── FriendRequestAdapter.kt  (Solicitações)
│   │
│   ├── 📁 profile/
│   │   └── ProfileFragment.kt       (Perfil do usuário)
│   │
│   ├── 📁 mail/
│   │   └── MailFragment.kt          (Mensagens)
│   │
│   └── 📁 advice/
│       └── AdviceFragment.kt        (Conselhos)
│
├── 📁 util/                         ← Utilities
│   ├── Resource.kt                  (Sealed class para estados)
│   ├── TokenManager.kt              (Gestão JWT)
│   ├── NetworkUtils.kt              (Check conectividade)
│   └── DateUtils.kt                 (Formatação datas)
│
└── MainActivity.kt                  ← Entry Point (Login/Register)

📁 app/src/main/res/
│
├── 📁 layout/                       ← XML Layouts
│   ├── activity_main.xml            (Tela de login)
│   ├── activity_feed.xml            (Feed principal)
│   ├── fragment_feed.xml
│   ├── fragment_friends.xml
│   ├── fragment_profile.xml
│   ├── item_post.xml                (Card de post)
│   ├── item_friend.xml              (Card de amigo)
│   └── item_friend_request.xml
│
├── 📁 values/
│   ├── colors.xml                   (Cores do tema)
│   ├── strings.xml                  (Textos)
│   └── themes.xml                   (Material Theme)
│
└── 📁 drawable/                     ← Ícones e imagens
    ├── ic_heart_outline.xml
    ├── ic_heart_filled.xml
    ├── ic_comment.xml
    └── ic_avatar_placeholder.xml
```

---

## 📦 CAMADAS E RESPONSABILIDADES

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI LAYER                                │
│  Responsabilidade:                                              │
│  • Renderizar interface                                         │
│  • Capturar eventos do usuário                                  │
│  • Observar LiveData                                            │
│  • Atualizar views com dados                                    │
│  • Navegação entre telas                                        │
│                                                                 │
│  NÃO FAZ:                                                       │
│  ✗ Lógica de negócio                                           │
│  ✗ Chamadas de API diretas                                     │
│  ✗ Manipulação de dados complexa                               │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      VIEWMODEL LAYER                            │
│  Responsabilidade:                                              │
│  • Lógica de apresentação                                       │
│  • Gerenciar estado da UI                                       │
│  • Expor LiveData para UI                                       │
│  • Validar inputs                                               │
│  • Coordenar Repository                                         │
│  • Sobrevive a mudanças de configuração                         │
│                                                                 │
│  NÃO FAZ:                                                       │
│  ✗ Manipular Views diretamente                                 │
│  ✗ Conhecer Activity/Fragment                                  │
│  ✗ Fazer chamadas HTTP diretas                                 │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REPOSITORY LAYER                           │
│  Responsabilidade:                                              │
│  • Single source of truth                                       │
│  • Centralizar acesso a dados                                   │
│  • Abstrair origem dos dados (API, DB, Cache)                   │
│  • Processar responses                                          │
│  • Error handling                                               │
│  • Encapsular em Resource<T>                                    │
│                                                                 │
│  NÃO FAZ:                                                       │
│  ✗ Lógica de UI                                                │
│  ✗ Validação de inputs da UI                                   │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DATA SOURCE LAYER                         │
│  Responsabilidade:                                              │
│  • Fazer requisições HTTP                                       │
│  • Converter JSON ↔ Kotlin                                      │
│  • Gerenciar conexões                                           │
│  • Timeout e retry                                              │
│  • Logging (em debug)                                           │
│                                                                 │
│  NÃO FAZ:                                                       │
│  ✗ Lógica de negócio                                           │
│  ✗ Processamento de dados                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 FLUXO DE AUTENTICAÇÃO

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                          │
└─────────────────────────────────────────────────────────────────┘

1️⃣  REGISTRO
   ┌─────────┐  Nome, Email, Senha
   │ Usuario │──────────────────────┐
   └─────────┘                      │
                                    ▼
                          ┌──────────────────┐
                          │  POST /register  │
                          │  Backend valida  │
                          └────────┬─────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                 Sucesso                       Falha
                    │                             │
                    ▼                             ▼
         ┌────────────────────┐      ┌──────────────────┐
         │ Gera JWT Token     │      │ Retorna erro     │
         │ Salva no DB        │      │ "Email já existe"│
         │ Retorna AuthResp   │      └──────────────────┘
         └─────────┬──────────┘
                   │
                   ▼
         ┌────────────────────┐
         │ TokenManager       │
         │  .saveToken()      │
         └─────────┬──────────┘
                   │
                   ▼
         ┌────────────────────┐
         │ Navega para Feed   │
         └────────────────────┘

2️⃣  LOGIN
   ┌─────────┐  Email, Senha
   │ Usuario │─────────────────────┐
   └─────────┘                     │
                                   ▼
                         ┌─────────────────┐
                         │  POST /login    │
                         │  Backend verifica│
                         └────────┬────────┘
                                  │
                   ┌──────────────┴──────────────┐
                   │                             │
                Sucesso                       Falha
                   │                             │
                   ▼                             ▼
        ┌────────────────────┐      ┌──────────────────┐
        │ Retorna JWT        │      │ "Credenciais     │
        │ + Dados do User    │      │  inválidas"      │
        └─────────┬──────────┘      └──────────────────┘
                  │
                  ▼
        ┌────────────────────┐
        │ TokenManager       │
        │  .saveToken()      │
        │  .saveUserInfo()   │
        └─────────┬──────────┘
                  │
                  ▼
        ┌────────────────────┐
        │ Navega para Feed   │
        └────────────────────┘

3️⃣  REQUISIÇÕES AUTENTICADAS
   ┌─────────────────────────┐
   │ Toda requisição após    │
   │ login inclui header:    │
   │                         │
   │ Authorization:          │
   │ Bearer eyJhbGciOi...    │
   └────────────┬────────────┘
                │
                ▼
   ┌─────────────────────────┐
   │ Backend valida token    │
   │ • Verifica assinatura   │
   │ • Verifica expiração    │
   │ • Extrai userId         │
   └────────────┬────────────┘
                │
     ┌──────────┴──────────┐
     │                     │
  Válido                Inválido
     │                     │
     ▼                     ▼
┌─────────┐       ┌────────────────┐
│ Processa│       │ 401 Unauthorized│
│ Request │       │ Força logout   │
└─────────┘       └────────────────┘

4️⃣  LOGOUT
   ┌─────────┐
   │ Usuario │ Clica em "Sair"
   └────┬────┘
        │
        ▼
   ┌─────────────────────┐
   │ TokenManager        │
   │  .clearToken()      │
   │  .clearUserInfo()   │
   └─────────┬───────────┘
             │
             ▼
   ┌─────────────────────┐
   │ Volta para tela de  │
   │ Login (MainActivity)│
   └─────────────────────┘
```

---

## 🎯 RESOURCE PATTERN (Estados)

```
┌─────────────────────────────────────────────────────────────────┐
│                        RESOURCE<T>                              │
│                      (Sealed Class)                             │
└─────────────────────────────────────────────────────────────────┘

sealed class Resource<T>(
    val data: T? = null,
    val message: String? = null
)

├── Success<T>(data: T)
│   └── Quando requisição é bem-sucedida
│       • data: contém os dados retornados
│       • Exemplo: Resource.Success(listOf(post1, post2))
│
├── Error<T>(message: String, data: T? = null)
│   └── Quando ocorre erro
│       • message: descrição do erro
│       • data: dados parciais (opcional)
│       • Exemplo: Resource.Error("Sem conexão")
│
└── Loading<T>()
    └── Enquanto requisição está em andamento
        • Sem dados ainda
        • Exemplo: Resource.Loading()

┌─────────────────────────────────────────────────────────────────┐
│                    FLUXO DE ESTADOS                             │
└─────────────────────────────────────────────────────────────────┘

Requisição Iniciada
       │
       ▼
┌──────────────┐
│   Loading    │ ← _feedState.value = Resource.Loading()
└──────┬───────┘
       │
       │ API Request...
       │
       ├─────────────┬─────────────┐
       │             │             │
    Sucesso       Erro 1        Erro 2
       │             │             │
       ▼             ▼             ▼
┌──────────┐  ┌───────────┐  ┌───────────┐
│ Success  │  │   Error   │  │   Error   │
│ (data)   │  │ (network) │  │ (server)  │
└──────────┘  └───────────┘  └───────────┘
       │             │             │
       └─────────────┴─────────────┘
                     │
                     ▼
            ┌────────────────┐
            │  UI observa e  │
            │  atualiza view │
            └────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    USO NA PRÁTICA                               │
└─────────────────────────────────────────────────────────────────┘

viewModel.feedState.observe(this) { resource ->
    when (resource) {
        is Resource.Loading -> {
            progressBar.visibility = View.VISIBLE
            recyclerView.visibility = View.GONE
            errorView.visibility = View.GONE
        }
        
        is Resource.Success -> {
            progressBar.visibility = View.GONE
            recyclerView.visibility = View.VISIBLE
            errorView.visibility = View.GONE
            
            resource.data?.let { posts ->
                adapter.submitList(posts)
            }
        }
        
        is Resource.Error -> {
            progressBar.visibility = View.GONE
            recyclerView.visibility = View.GONE
            errorView.visibility = View.VISIBLE
            errorText.text = resource.message
            
            retryButton.setOnClickListener {
                viewModel.loadFeed()
            }
        }
    }
}
```

---

## 📊 RESUMO DA STACK TECNOLÓGICA

```
┌─────────────────────────────────────────────────────────────┐
│                    TECH STACK                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Linguagem:        Kotlin 100%                              │
│  IDE:              Android Studio                           │
│  Min SDK:          24 (Android 7.0)                         │
│  Target SDK:       34 (Android 14)                          │
│  Compile SDK:      34                                       │
│                                                             │
│  Arquitetura:      MVVM                                     │
│  Async:            Kotlin Coroutines                        │
│  DI:               Manual (preparado para Hilt)             │
│  Network:          Retrofit 2.9.0 + OkHttp 4.12.0           │
│  Serialization:    Gson                                     │
│  Image Loading:    Coil                                     │
│  UI:               Material Design 3                        │
│  ViewBinding:      Habilitado                               │
│  Navigation:       Navigation Component                     │
│  Persistence:      SharedPreferences (token)                │
│  Database:         Room (preparado, não implementado)       │
│                                                             │
│  Testing:          JUnit, Espresso (pendente)               │
│  CI/CD:            Não configurado                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**📝 Conclusão:** Arquitetura sólida, escalável e seguindo as melhores práticas do Android moderno! 🚀

